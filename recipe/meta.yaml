{% set name = "sqlean.py" %}
{% set version = "3.47.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/sqlean_py-{{ version }}.tar.gz
  sha256: 86a540b2d9ca44ae2b0b56fb067bf13095945a9bf113b3ea623e4867aab16ac2

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  build:
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
  host:
    - python
    - pip
  run:
    - python

test:
  imports:
    - sqlean
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/nalgeon/sqlean.py
  summary: sqlite3 with extensions
  license: Zlib
  license_file: LICENSE

extra:
  recipe-maintainers:
    - anticorrelator
    - axiomofjoy
    - mikeldking
    - RogerHYang
